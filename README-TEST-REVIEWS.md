# 🧪 دليل اختبار صفحة إدارة التقييمات

## كيفية تشغيل الاختبار

### الطريقة 1: من مجلد المشروع الرئيسي
```bash
node test-reviews-page.js
```

### الطريقة 2: من مجلد backend
```bash
cd backend
npm run test:reviews
```

### الطريقة 3: مع متغيرات البيئة
```bash
API_URL=http://localhost:3007/api/v1 \
TEST_EMAIL=ali@ali.com \
TEST_PASSWORD=password \
node test-reviews-page.js
```

## المتطلبات

1. **تثبيت axios** (إذا لم يكن مثبتاً):
```bash
npm install axios
```

2. **تشغيل الخادم الخلفي**:
```bash
cd backend
npm start
```

3. **تعديل بيانات الاختبار**:
   - افتح `test-reviews-page.js`
   - عدل `TEST_EMAIL` و `TEST_PASSWORD` حسب بياناتك

## ما يختبره السكربت

1. ✅ **تسجيل الدخول** - يتحقق من أن API تسجيل الدخول يعمل
2. ✅ **التحقق من المصادقة** - يتحقق من `/auth/me` endpoint
3. ✅ **جلب التقييمات** - يتحقق من `GET /api/v1/reviews` endpoint
4. ✅ **تسجيل الـ routes** - يتحقق من أن الـ routes مسجلة بشكل صحيح
5. ✅ **Frontend Route** - يتحقق من أن الـ route موجود في App.tsx
6. ✅ **قاعدة البيانات** - يتحقق من اتصال قاعدة البيانات

## تفسير النتائج

### ✅ نجح الاختبار
- يعني أن كل شيء يعمل بشكل صحيح
- يمكنك الوصول إلى `/products/reviews` بدون مشاكل

### ❌ فشل الاختبار
- **فشل تسجيل الدخول**: تحقق من بيانات المستخدم
- **فشل /auth/me**: تحقق من JWT token
- **فشل GET /reviews**: هذه هي المشكلة الرئيسية
  - تحقق من `server.js` - يجب أن يحتوي على `app.use("/api/v1/reviews", productReviewRoutes)`
  - تحقق من `productReviewRoutes.js` - يجب أن يحتوي على `router.get("/", productReviewController.getAllReviews)`
  - تحقق من `globalSecurity` middleware
  - تحقق من console الخادم لمعرفة الأخطاء

## حل المشاكل الشائعة

### خطأ 404 (Not Found)
```
❌ فشل جلب التقييمات: Request failed with status code 404
```
**الحل**: تحقق من أن الـ route مسجل في `server.js`

### خطأ 401 (Unauthorized)
```
❌ فشل جلب التقييمات: Request failed with status code 401
```
**الحل**: تحقق من أن `globalSecurity` middleware يطبق بشكل صحيح

### خطأ 500 (Internal Server Error)
```
❌ فشل جلب التقييمات: Request failed with status code 500
```
**الحل**: تحقق من `productReviewController.js` و console الخادم

## ملاحظات

- السكربت يطبع نتائج ملونة في Console
- ✅ = نجح
- ❌ = فشل
- ⚠️ = تحذير
- ℹ️ = معلومات

