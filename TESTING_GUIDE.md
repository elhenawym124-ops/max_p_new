# 🧪 دليل اختبار المزايا الجديدة - Storefront Features

## 📋 نظرة عامة

هذا الدليل يوضح كيفية اختبار جميع المزايا الجديدة التي تم إضافتها للمتجر الإلكتروني.

---

## 🚀 البدء السريع

### 1. تشغيل السكريبت التلقائي

```bash
# في PowerShell
cd backend
node testStorefrontFeatures.js
```

### 2. اختبار يدوي

اتبع الخطوات أدناه لاختبار كل ميزة يدوياً.

---

## 🧪 اختبار Backend APIs

### 1. Storefront Settings API

#### 1.1 GET Settings (Protected)
```bash
GET http://localhost:3007/api/v1/storefront-settings
Headers:
  Authorization: Bearer YOUR_TOKEN
```

**النتيجة المتوقعة:**
```json
{
  "success": true,
  "data": {
    "id": "...",
    "companyId": "...",
    "quickViewEnabled": true,
    "comparisonEnabled": true,
    ...
  }
}
```

#### 1.2 PUT Settings (Protected)
```bash
PUT http://localhost:3007/api/v1/storefront-settings
Headers:
  Authorization: Bearer YOUR_TOKEN
  Content-Type: application/json
Body:
{
  "quickViewEnabled": true,
  "comparisonEnabled": true,
  "wishlistEnabled": true,
  "supportedLanguages": ["ar", "en"]
}
```

#### 1.3 GET Public Settings
```bash
GET http://localhost:3007/api/v1/public/storefront-settings/:companyId
```

---

### 2. Quick View API

```bash
GET http://localhost:3007/api/v1/public/products/:productId/quick
```

**النتيجة المتوقعة:**
```json
{
  "success": true,
  "data": {
    "id": "...",
    "name": "Product Name",
    "price": 100,
    "images": ["url1", "url2"],
    "stock": 10,
    ...
  }
}
```

---

### 3. Wishlist API

#### 3.1 Add to Wishlist
```bash
POST http://localhost:3007/api/v1/public/wishlist
Content-Type: application/json
Body:
{
  "productId": "product_id",
  "sessionId": "session_id"
}
```

#### 3.2 Get Wishlist
```bash
GET http://localhost:3007/api/v1/public/wishlist?sessionId=session_id
```

#### 3.3 Remove from Wishlist
```bash
DELETE http://localhost:3007/api/v1/public/wishlist/:wishlistId
```

---

### 4. Product Reviews API

#### 4.1 Get Reviews
```bash
GET http://localhost:3007/api/v1/public/products/:productId/reviews
```

#### 4.2 Create Review
```bash
POST http://localhost:3007/api/v1/public/products/:productId/reviews
Content-Type: application/json
Body:
{
  "rating": 5,
  "comment": "منتج رائع!",
  "customerName": "Customer Name",
  "customerEmail": "customer@example.com"
}
```

---

### 5. Back in Stock API

```bash
POST http://localhost:3007/api/v1/public/back-in-stock
Content-Type: application/json
Body:
{
  "productId": "product_id",
  "customerName": "Customer Name",
  "customerEmail": "customer@example.com",
  "notifyEmail": true,
  "notifySMS": false
}
```

---

### 6. Recently Viewed API

#### 6.1 Record View
```bash
POST http://localhost:3007/api/v1/public/recently-viewed
Content-Type: application/json
Body:
{
  "productId": "product_id",
  "sessionId": "session_id"
}
```

#### 6.2 Get Recently Viewed
```bash
GET http://localhost:3007/api/v1/public/recently-viewed?sessionId=session_id
```

---

## 🎨 اختبار Frontend Components

### 1. Settings Page (`/settings/storefront-features`)

#### الخطوات:
1. افتح `/settings/storefront-features` في المتصفح
2. تأكد من تسجيل الدخول
3. تحقق من:
   - ✅ تحميل الإعدادات من API
   - ✅ عرض جميع الأقسام (Quick View, Comparison, Wishlist, etc.)
   - ✅ تفعيل/إلغاء تفعيل كل ميزة
   - ✅ حفظ الإعدادات (زر "حفظ التغييرات")
   - ✅ إعادة تعيين الإعدادات (زر "إعادة تعيين")

#### الاختبارات:
- [ ] تفعيل Quick View → حفظ → التحقق من التفعيل
- [ ] تعطيل Comparison → حفظ → التحقق من التعطيل
- [ ] تغيير `maxComparisonProducts` → حفظ → التحقق من التغيير
- [ ] إعادة تعيين → التحقق من العودة للقيم الافتراضية

---

### 2. Shop Page (`/shop`)

#### الخطوات:
1. افتح `/shop` في المتصفح
2. تحقق من:
   - ✅ عرض المنتجات
   - ✅ Quick View (زر "معاينة سريعة" على كل منتج)
   - ✅ Advanced Filters (زر "فلاتر متقدمة")
   - ✅ Product Comparison (زر "مقارنة" على كل منتج)
   - ✅ Product Badges (شارات: جديد، الأكثر مبيعاً، في عرض)
   - ✅ Countdown Timer (على المنتجات في عرض)
   - ✅ Recently Viewed (قسم في الأسفل)

#### الاختبارات التفصيلية:

##### Quick View:
- [ ] النقر على "معاينة سريعة" → فتح Modal
- [ ] عرض صور المنتج في Modal
- [ ] إضافة للسلة من Modal
- [ ] إضافة للمفضلة من Modal (إذا كان مفعلاً)
- [ ] إغلاق Modal

##### Advanced Filters:
- [ ] فتح الفلاتر → عرض جميع الخيارات
- [ ] فلترة بالسعر → التحقق من النتائج
- [ ] فلترة بالتقييم → التحقق من النتائج
- [ ] فلترة بالعلامة التجارية → التحقق من النتائج
- [ ] إعادة تعيين الفلاتر

##### Product Comparison:
- [ ] إضافة منتج للمقارنة → ظهور في نافذة المقارنة
- [ ] إضافة منتج ثاني → ظهور في المقارنة
- [ ] الوصول للحد الأقصى → رسالة تحذير
- [ ] مقارنة المنتجات → عرض السعر والمواصفات
- [ ] إزالة منتج من المقارنة

---

### 3. Product Details Page (`/shop/products/:id`)

#### الخطوات:
1. افتح صفحة منتج معين
2. تحقق من:
   - ✅ Product Image Zoom (hover/click على الصورة)
   - ✅ Product Videos (إذا كان المنتج يحتوي على فيديو)
   - ✅ Size Guide (زر "دليل المقاسات")
   - ✅ Wishlist Button (زر القلب)
   - ✅ Product Reviews (قسم التقييمات)
   - ✅ Countdown Timer (إذا كان المنتج في عرض)
   - ✅ Back in Stock (إذا كان المنتج غير متوفر)
   - ✅ Social Sharing (أزرار المشاركة)
   - ✅ Product Badges (الشارات)
   - ✅ Product Tabs (التبويبات: الوصف، المواصفات، التقييمات، الشحن)
   - ✅ Sticky Add to Cart (عند التمرير لأسفل)

#### الاختبارات التفصيلية:

##### Image Zoom:
- [ ] Hover على الصورة → تكبير الصورة
- [ ] Click على الصورة → فتح في نافذة منبثقة
- [ ] تغيير نوع Zoom من Settings → التحقق من التغيير

##### Wishlist:
- [ ] النقر على زر القلب → إضافة للمفضلة
- [ ] النقر مرة أخرى → إزالة من المفضلة
- [ ] فتح صفحة المفضلة → التحقق من ظهور المنتج

##### Product Reviews:
- [ ] عرض التقييمات الموجودة
- [ ] إضافة تقييم جديد → ملء النموذج
- [ ] إرسال التقييم → التحقق من الظهور
- [ ] تقييم مفيد (Helpful) → زيادة العدد

##### Product Tabs:
- [ ] التبديل بين التبويبات
- [ ] عرض الوصف
- [ ] عرض المواصفات
- [ ] عرض التقييمات
- [ ] عرض معلومات الشحن

##### Sticky Add to Cart:
- [ ] التمرير لأسفل → ظهور الزر الثابت
- [ ] النقر على الزر → إضافة للسلة
- [ ] التمرير لأعلى → إخفاء الزر

---

### 4. Wishlist Page (`/shop/wishlist`)

#### الخطوات:
1. افتح `/shop/wishlist`
2. تحقق من:
   - ✅ عرض المنتجات في المفضلة
   - ✅ إزالة منتج من المفضلة
   - ✅ إضافة منتج للسلة من المفضلة

#### الاختبارات:
- [ ] عرض المنتجات المضافة للمفضلة
- [ ] النقر على "إزالة" → إزالة المنتج
- [ ] النقر على "إضافة للسلة" → إضافة المنتج للسلة

---

### 5. Navigation (`StorefrontNav`)

#### الخطوات:
1. افتح أي صفحة في المتجر
2. تحقق من:
   - ✅ Language Switcher (مبدل اللغة)
   - ✅ Cart Icon (أيقونة السلة مع العدد)
   - ✅ Cart Preview (عند hover على السلة)

#### الاختبارات:
- [ ] تغيير اللغة → التحقق من التغيير
- [ ] إضافة منتج للسلة → زيادة العدد
- [ ] Hover على السلة → عرض معاينة السلة

---

## 🔗 اختبار التكامل

### 1. Settings → Components

#### الاختبار:
1. افتح Settings Page
2. تعطيل Quick View → حفظ
3. افتح Shop Page
4. التحقق من عدم ظهور زر "معاينة سريعة"

#### قائمة الاختبارات:
- [ ] تعطيل Quick View → عدم ظهوره في Shop
- [ ] تعطيل Wishlist → إخفاء أزرار المفضلة
- [ ] تعطيل Comparison → إخفاء أزرار المقارنة
- [ ] تعطيل Reviews → إخفاء قسم التقييمات
- [ ] تعطيل Filters → إخفاء الفلاتر
- [ ] تعطيل Recently Viewed → إخفاء القسم

---

### 2. Data Flow

#### الاختبارات:
- [ ] إضافة منتج للمفضلة → فتح صفحة المفضلة → التحقق من الظهور
- [ ] إضافة منتج للمقارنة → فتح نافذة المقارنة → التحقق من الظهور
- [ ] عرض منتج → فتح Shop Page → التحقق من ظهوره في Recently Viewed
- [ ] إضافة تقييم → فتح صفحة المنتج → التحقق من ظهور التقييم

---

## 🐛 اختبار Edge Cases

### 1. Error Handling

#### الاختبارات:
- [ ] API Error (500) → عرض رسالة خطأ
- [ ] Network Error → عرض رسالة خطأ
- [ ] Invalid Data → التحقق من التحقق من البيانات

### 2. Limits

#### الاختبارات:
- [ ] Wishlist Max Items → إضافة أكثر من الحد → رسالة تحذير
- [ ] Comparison Max Products → إضافة أكثر من الحد → رسالة تحذير
- [ ] Recently Viewed Count → التحقق من العدد المحدد

### 3. Guest vs Authenticated

#### الاختبارات:
- [ ] Guest User → إضافة للمفضلة → التحقق من العمل
- [ ] Guest User → إضافة للمقارنة → التحقق من العمل
- [ ] Authenticated User → إضافة للمفضلة → التحقق من الحفظ في DB

---

## 📊 سجل الاختبار

### Backend APIs
- [ ] Storefront Settings API
- [ ] Quick View API
- [ ] Wishlist API
- [ ] Product Reviews API
- [ ] Back in Stock API
- [ ] Recently Viewed API

### Frontend Components
- [ ] Settings Page
- [ ] Shop Page
- [ ] Product Details Page
- [ ] Wishlist Page
- [ ] Navigation

### Integration
- [ ] Settings → Components
- [ ] Data Flow

### Edge Cases
- [ ] Error Handling
- [ ] Limits
- [ ] Guest vs Authenticated

---

## ✅ النتيجة النهائية

بعد إكمال جميع الاختبارات، سجل النتائج هنا:

**تاريخ الاختبار:** _______________

**المختبر:** _______________

**النتيجة الإجمالية:** ⬜ 0/16

**ملاحظات:**
- 

---

## 🆘 حل المشاكل

### مشكلة: API يعيد 401 Unauthorized
**الحل:** تأكد من تسجيل الدخول والحصول على Token صحيح

### مشكلة: Component لا يظهر
**الحل:** تحقق من Settings → تأكد من تفعيل الميزة

### مشكلة: Data لا يتم حفظها
**الحل:** تحقق من Console للأخطاء → تحقق من Backend Logs

---

## 📞 الدعم

إذا واجهت أي مشاكل أثناء الاختبار، راجع:
- `TESTING_PLAN.md` - خطة الاختبار التفصيلية
- `backend/testStorefrontFeatures.js` - سكريبت الاختبار التلقائي
- Console Logs في المتصفح
- Backend Logs في Terminal

