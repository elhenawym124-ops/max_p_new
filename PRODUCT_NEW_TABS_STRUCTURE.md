# 📑 هيكل صفحة إضافة المنتج الجديد بنظام Tabs

## 🎯 الهدف
إعادة تنظيم صفحة `/products/new` لتكون أكثر سهولة وقابلية للتوسع باستخدام نظام التبويبات (Tabs).

---

## 📊 التبويبات المقترحة

### 1️⃣ **المعلومات الأساسية** (Basic Info)
**الأيقونة**: 📝  
**المحتوى**:
- اسم المنتج
- الوصف (Rich Text Editor في المستقبل)
- رمز المنتج (SKU)
- الفئة
- حالة المنتج (نشط/غير نشط)

---

### 2️⃣ **التسعير** (Pricing)
**الأيقونة**: 💰  
**المحتوى**:
- السعر الأساسي
- السعر القديم (Compare Price)
- سعر التكلفة (Cost)
- تواريخ العروض:
  - تاريخ بداية العرض
  - تاريخ انتهاء العرض
- حساب الربح تلقائياً (السعر - التكلفة)

---

### 3️⃣ **المخزون** (Inventory)
**الأيقونة**: 📦  
**المحتوى**:
- تفعيل تتبع المخزون
- الكمية المتاحة
- حد التنبيه للمخزون المنخفض
- حالة المخزون (In Stock, Out of Stock, On Backorder) - مستقبلاً

---

### 4️⃣ **الصور والوسائط** (Media)
**الأيقونة**: 🖼️  
**المحتوى**:
- رفع الصور
- معاينة الصور
- ترتيب الصور (Drag & Drop) - مستقبلاً
- تحديد الصورة الرئيسية - مستقبلاً
- نص بديل للصور (Alt Text) - مستقبلاً
- فيديوهات المنتج - مستقبلاً

---

### 5️⃣ **المتغيرات** (Variants)
**الأيقونة**: 🎨  
**المحتوى**:
- قائمة المتغيرات (ألوان، مقاسات، إلخ)
- إضافة متغير جديد
- تعديل/حذف المتغيرات
- كل متغير له:
  - الاسم والنوع
  - SKU خاص
  - السعر (اختياري)
  - المخزون
  - الصور الخاصة

---

### 6️⃣ **الشحن** (Shipping)
**الأيقونة**: 🚚  
**المحتوى**:
- الوزن
- الأبعاد (الطول، العرض، الارتفاع)
- فئة الشحن - مستقبلاً
- تكلفة الشحن الخاصة - مستقبلاً

---

### 7️⃣ **إعدادات متقدمة** (Advanced)
**الأيقونة**: ⚙️  
**المحتوى**:
- تفعيل فورم الشيك أوت
- إظهار/إخفاء زر السلة
- العلامات (Tags)
- SEO (مستقبلاً):
  - Meta Title
  - Meta Description
  - Keywords
  - Slug
- منتجات مشابهة - مستقبلاً
- Up-sells & Cross-sells - مستقبلاً

---

## 🎨 التصميم المقترح

```
┌─────────────────────────────────────────────────────────────┐
│  ← رجوع          إضافة منتج جديد                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌───────┬───────┬───────┬───────┬───────┬───────┬───────┐ │
│  │ 📝    │ 💰    │ 📦    │ 🖼️    │ 🎨    │ 🚚    │ ⚙️    │ │
│  │ أساسي │ تسعير │ مخزون │ صور   │ متغيرات│ شحن  │ متقدم │ │
│  └───────┴───────┴───────┴───────┴───────┴───────┴───────┘ │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │                                                       │ │
│  │  [محتوى التبويب النشط]                               │ │
│  │                                                       │ │
│  │                                                       │ │
│  │                                                       │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                             │
│                                    [إلغاء]  [حفظ المنتج]  │
└─────────────────────────────────────────────────────────────┘
```

---

## 💻 الكود المطلوب

### 1. مكون Tabs (✅ تم إنشاؤه)
الملف: `frontend/src/components/common/Tabs.tsx`

### 2. تعديل ProductNew.tsx

```typescript
import { Tabs, TabPanel } from '../../components/common/Tabs';
import { 
  DocumentTextIcon,
  CurrencyDollarIcon,
  CubeIcon,
  PhotoIcon,
  SwatchIcon,
  TruckIcon,
  Cog6ToothIcon
} from '@heroicons/react/24/outline';

const ProductNew: React.FC = () => {
  const [activeTab, setActiveTab] = useState('basic');
  
  const tabs = [
    { id: 'basic', label: 'المعلومات الأساسية', icon: <DocumentTextIcon className="w-5 h-5" /> },
    { id: 'pricing', label: 'التسعير', icon: <CurrencyDollarIcon className="w-5 h-5" /> },
    { id: 'inventory', label: 'المخزون', icon: <CubeIcon className="w-5 h-5" /> },
    { id: 'media', label: 'الصور', icon: <PhotoIcon className="w-5 h-5" /> },
    { id: 'variants', label: 'المتغيرات', icon: <SwatchIcon className="w-5 h-5" />, badge: variants.length },
    { id: 'shipping', label: 'الشحن', icon: <TruckIcon className="w-5 h-5" /> },
    { id: 'advanced', label: 'متقدم', icon: <Cog6ToothIcon className="w-5 h-5" /> },
  ];

  return (
    <form onSubmit={handleSubmit}>
      <Tabs tabs={tabs} activeTab={activeTab} onTabChange={setActiveTab}>
        
        {/* Tab 1: Basic Info */}
        <TabPanel id="basic" activeTab={activeTab}>
          {/* اسم المنتج، الوصف، SKU، الفئة */}
        </TabPanel>

        {/* Tab 2: Pricing */}
        <TabPanel id="pricing" activeTab={activeTab}>
          {/* السعر، السعر القديم، التكلفة، تواريخ العروض */}
        </TabPanel>

        {/* Tab 3: Inventory */}
        <TabPanel id="inventory" activeTab={activeTab}>
          {/* تتبع المخزون، الكمية، حد التنبيه */}
        </TabPanel>

        {/* Tab 4: Media */}
        <TabPanel id="media" activeTab={activeTab}>
          {/* رفع الصور، معاينة */}
        </TabPanel>

        {/* Tab 5: Variants */}
        <TabPanel id="variants" activeTab={activeTab}>
          {/* قائمة المتغيرات */}
        </TabPanel>

        {/* Tab 6: Shipping */}
        <TabPanel id="shipping" activeTab={activeTab}>
          {/* الوزن، الأبعاد */}
        </TabPanel>

        {/* Tab 7: Advanced */}
        <TabPanel id="advanced" activeTab={activeTab}>
          {/* الإعدادات المتقدمة، Tags، SEO */}
        </TabPanel>

      </Tabs>

      {/* أزرار الحفظ والإلغاء */}
      <div className="mt-6 flex justify-end gap-3">
        <button type="button" onClick={() => navigate('/products')}>إلغاء</button>
        <button type="submit">حفظ المنتج</button>
      </div>
    </form>
  );
};
```

---

## ✨ المميزات

### 1. **تنظيم أفضل**
- كل مجموعة من الحقول في تبويب منفصل
- سهولة الوصول للمعلومات
- تقليل الازدحام البصري

### 2. **قابلية التوسع**
- إضافة تبويبات جديدة بسهولة
- مثال: تبويب "SEO" في المستقبل
- مثال: تبويب "Related Products"

### 3. **تجربة مستخدم أفضل**
- التركيز على مجموعة واحدة من الحقول
- تقليل التمرير (Scrolling)
- واجهة احترافية

### 4. **Validation ذكي**
- عرض Badge على التبويبات التي بها أخطاء
- مثال: إذا السعر فارغ، يظهر تنبيه على تبويب "التسعير"

---

## 🚀 خطة التنفيذ

### المرحلة 1: الأساسيات (يوم واحد)
- [x] إنشاء مكون Tabs
- [ ] تقسيم ProductNew.tsx إلى تبويبات
- [ ] اختبار الوظائف الأساسية

### المرحلة 2: التحسينات (يومين)
- [ ] إضافة Validation لكل تبويب
- [ ] إضافة Progress Indicator
- [ ] تطبيق نفس النظام على ProductEditNew.tsx

### المرحلة 3: الميزات المتقدمة (أسبوع)
- [ ] إضافة تبويب SEO
- [ ] Rich Text Editor للوصف
- [ ] Image Gallery متقدم
- [ ] Drag & Drop للصور

---

## 📝 ملاحظات مهمة

### حفظ حالة التبويبات
```typescript
// حفظ التبويب النشط في localStorage
useEffect(() => {
  const savedTab = localStorage.getItem('productNewActiveTab');
  if (savedTab) setActiveTab(savedTab);
}, []);

useEffect(() => {
  localStorage.setItem('productNewActiveTab', activeTab);
}, [activeTab]);
```

### Validation
```typescript
const validateTab = (tabId: string): boolean => {
  switch(tabId) {
    case 'basic':
      return !!(formData.name && formData.category);
    case 'pricing':
      return formData.price > 0;
    case 'inventory':
      return !formData.trackInventory || formData.stock >= 0;
    default:
      return true;
  }
};

// عرض علامة تحذير على التبويبات غير المكتملة
const getTabBadge = (tabId: string) => {
  if (!validateTab(tabId)) return '!';
  return undefined;
};
```

### Navigation بين التبويبات
```typescript
const goToNextTab = () => {
  const currentIndex = tabs.findIndex(t => t.id === activeTab);
  if (currentIndex < tabs.length - 1) {
    setActiveTab(tabs[currentIndex + 1].id);
  }
};

const goToPreviousTab = () => {
  const currentIndex = tabs.findIndex(t => t.id === activeTab);
  if (currentIndex > 0) {
    setActiveTab(tabs[currentIndex - 1].id);
  }
};
```

---

## 🎯 النتيجة المتوقعة

بعد التطبيق، ستكون الصفحة:
- ✅ أكثر تنظيماً وسهولة في الاستخدام
- ✅ قابلة للتوسع بسهولة
- ✅ احترافية المظهر
- ✅ تجربة مستخدم ممتازة
- ✅ سهلة الصيانة والتطوير

---

**هل تريد أن أبدأ في تطبيق هذا التصميم على ProductNew.tsx؟**
