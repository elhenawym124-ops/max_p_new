# إعداد Telegram Userbot - دليل خطوة بخطوة

## 📋 المتطلبات

للاستخدام Telegram Userbot، تحتاج إلى:
1. **TELEGRAM_API_ID** - معرف API من Telegram
2. **TELEGRAM_API_HASH** - Hash API من Telegram

---

## 🔑 كيفية الحصول على API ID و API Hash

### الخطوة 1: إنشاء تطبيق Telegram

1. افتح المتصفح واذهب إلى: **https://my.telegram.org/apps**
2. سجل الدخول بحساب Telegram الخاص بك (استخدم رقم الهاتف)
3. إذا لم يكن لديك تطبيق، انقر على **"Create new application"**

### الخطوة 2: ملء بيانات التطبيق

- **App title**: أي اسم (مثلاً: "My Userbot App")
- **Short name**: أي اسم قصير (مثلاً: "userbot")
- **Platform**: اختر "Other"
- **Description**: (اختياري) وصف للتطبيق

### الخطوة 3: الحصول على API Credentials

بعد إنشاء التطبيق، ستحصل على:
- **api_id**: رقم (مثلاً: `12345678`)
- **api_hash**: نص طويل (مثلاً: `abcdef1234567890abcdef1234567890`)

⚠️ **مهم**: احتفظ بهذه القيم في مكان آمن ولا تشاركها مع أحد!

---

## ⚙️ إضافة المتغيرات إلى `.env`

### 1. افتح ملف `.env` في مجلد `backend/`

```bash
backend/.env
```

### 2. أضف المتغيرات التالية:

```env
# Telegram Userbot API Credentials
TELEGRAM_API_ID=your_api_id_here
TELEGRAM_API_HASH=your_api_hash_here
```

### 3. مثال:

```env
# Telegram Userbot API Credentials
TELEGRAM_API_ID=12345678
TELEGRAM_API_HASH=abcdef1234567890abcdef1234567890
```

---

## 🔄 إعادة تشغيل السيرفر

بعد إضافة المتغيرات:

1. **أوقف السيرفر** (إذا كان يعمل)
2. **أعد تشغيل السيرفر**:
   ```bash
   cd backend
   npm start
   # أو
   node server.js
   ```

---

## ✅ التحقق من الإعداد

1. افتح الصفحة: `http://localhost:3000/telegram-userbot`
2. يجب أن تختفي رسالة الخطأ الحمراء
3. يجب أن يعمل زر "Send Code" بشكل صحيح

---

## 🆘 استكشاف الأخطاء

### الخطأ: "Server configuration error: Missing TELEGRAM_API_ID/HASH"

**الحل:**
- تأكد من إضافة المتغيرات في ملف `.env` في مجلد `backend/`
- تأكد من عدم وجود مسافات حول `=` في `.env`
- أعد تشغيل السيرفر بعد إضافة المتغيرات

### الخطأ: "Invalid API ID or Hash"

**الحل:**
- تأكد من نسخ القيم بشكل صحيح
- تأكد من عدم وجود مسافات إضافية
- جرب إنشاء تطبيق جديد من https://my.telegram.org/apps

### الخطأ: "Connection timeout"

**الحل:**
- تحقق من اتصال الإنترنت
- تأكد من أن Telegram غير محظور في شبكتك
- جرب استخدام VPN إذا لزم الأمر

---

## 📝 ملاحظات مهمة

1. **API ID و Hash خاصان بك**: لا تشاركهم مع أحد
2. **يمكن استخدام نفس API ID/Hash لعدة تطبيقات**: لا حاجة لإنشاء تطبيق جديد لكل استخدام
3. **API ID/Hash لا تنتهي صلاحيتها**: يمكن استخدامها لفترة طويلة
4. **إذا نسيت API Hash**: يمكنك إنشاء تطبيق جديد والحصول على hash جديد

---

## 🔐 الأمان

- ✅ **لا ترفع ملف `.env` إلى Git**
- ✅ **استخدم متغيرات البيئة في Production**
- ✅ **احتفظ بنسخة احتياطية من API credentials**

---

## 📞 الدعم

إذا واجهت مشاكل:
1. تحقق من Console في المتصفح
2. تحقق من Logs في السيرفر
3. تأكد من أن جميع المتغيرات موجودة وصحيحة

