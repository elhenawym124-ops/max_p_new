# 📦 تعليمات تحديث Facebook Business SDK

## ✅ تم تحديث package.json
تم تحديث `facebook-nodejs-business-sdk` من `^21.0.2` إلى `^22.0.0` في `package.json`

## 🚀 خطوات التحديث

### الطريقة 1: استخدام npm install (موصى بها)
```bash
cd backend
npm install
```

### الطريقة 2: تحديث المكتبة فقط
```bash
cd backend
npm install facebook-nodejs-business-sdk@^22.0.0
```

### الطريقة 3: استخدام Scripts الجاهزة

#### Windows PowerShell:
```powershell
cd backend
.\update-facebook-sdk.ps1
```

#### Windows CMD:
```cmd
cd backend
update-facebook-sdk.bat
```

## ✅ التحقق من التحديث

بعد التحديث، تحقق من الإصدار:
```bash
npm list facebook-nodejs-business-sdk
```

يجب أن ترى إصدار `22.x.x` أو أحدث.

## 📝 ملاحظات

- تأكد من أنك في مجلد `backend` قبل تشغيل الأوامر
- إذا ظهرت أخطاء، جرب حذف `node_modules` و `package-lock.json` ثم إعادة التثبيت:
  ```bash
  rm -rf node_modules package-lock.json
  npm install
  ```

## ✅ بعد التحديث

بعد تحديث المكتبة، جميع المزايا الجديدة جاهزة للاستخدام:
- ✅ Advantage+ Audience Management
- ✅ Advantage+ Placements
- ✅ Advantage+ Budget Optimization
- ✅ Individual Creative Enhancements
- ✅ Instagram Reels Ads
- ✅ WhatsApp Ads
- ✅ Ad Recommendations API
- ✅ Ad Quality Metrics


