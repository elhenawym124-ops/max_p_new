# دليل تشغيل اختبار الذكاء الاصطناعي

## طريقة التشغيل

### الطريقة 1: استخدام Node.js مباشرة

```bash
cd backend
node scripts/quickAITest.js
```

### الطريقة 2: استخدام ملف batch (Windows)

```bash
cd backend/scripts
run-test.bat
```

### الطريقة 3: استخدام الاختبار الكامل

```bash
cd backend
node scripts/runAITest.js
```

### الطريقة 4: اختبار نوع محدد

```bash
cd backend
node scripts/runAITest.js --intent=product_inquiry --difficulty=easy
```

## ما يفعله السكريبت

1. **البحث عن شركة التسويق** في قاعدة البيانات
2. **إنشاء محادثة اختبار** جديدة
3. **جلب أسئلة الاختبار** من جميع الأنواع
4. **إرسال الرسائل تلقائياً** للذكاء الاصطناعي
5. **فحص جودة الردود** والتحقق من أنها مناسبة
6. **عرض تقرير** شامل عن النتائج

## النتائج المتوقعة

### رسائل نجحت ✅
- تم الحصول على رد من AI
- الرد مناسب للـ intent
- الرد يحتوي على معلومات مفيدة

### رسائل فشلت ❌
- لم يتم الحصول على رد
- حدث خطأ في المعالجة
- الرد غير مناسب

### رسائل صامتة 🤐
- النظام اختار عدم الرد
- قد يكون بسبب إعدادات AI

## فحص جودة الردود

السكريبت يفحص:
- ✅ طول الرد (ليس قصير جداً أو طويل جداً)
- ✅ وجود الـ intent المطلوب
- ✅ وجود تحليل المشاعر
- ✅ مناسبة الرد للـ intent
- ✅ وقت المعالجة

## عرض النتائج

بعد انتهاء الاختبار، يمكنك:
1. **عرض المحادثة** في `/test-chat`
2. **مراجعة النتائج** في التقرير
3. **فحص الردود غير المناسبة** وتحديد المشاكل
4. **إنشاء تقرير شامل** باستخدام API

## نصائح

1. **ابدأ بأسئلة سهلة**: استخدم `--difficulty=easy` أولاً
2. **اختبر نوع واحد**: استخدم `--intent=greeting` لاختبار نوع محدد
3. **راقب وقت المعالجة**: إذا كان طويلاً، قد تحتاج إلى تحسين
4. **راجع الردود غير المناسبة**: لتحسين إعدادات AI

## استكشاف الأخطاء

### الخطأ: "Cannot find module"
**الحل**: تأكد من أنك في مجلد `backend`

### الخطأ: "Company not found"
**الحل**: تأكد من وجود شركة "شركة التسويق" في قاعدة البيانات

### الخطأ: "No products found"
**الحل**: تأكد من وجود منتجات للشركة

### الخطأ: "AI service error"
**الحل**: تأكد من وجود مفتاح Gemini API نشط

## الملفات

- `quickAITest.js` - اختبار سريع وبسيط
- `runAITest.js` - اختبار شامل مع فحص متقدم
- `run-test.bat` - ملف batch للتشغيل السريع (Windows)

## الدعم

للمساعدة، راجع:
- `backend/services/TEST_CHAT_SYSTEM_README.md`
- `backend/scripts/USAGE_GUIDE.md`

