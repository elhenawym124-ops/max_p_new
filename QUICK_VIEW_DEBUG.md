# 🔍 دليل تصحيح مشكلة المعاينة السريعة

**تاريخ الإنشاء:** $(Get-Date -Format "yyyy-MM-dd HH:mm:ss")

---

## 🐛 المشكلة

المستخدم فعل ميزة "المعاينة السريعة" في الإعدادات لكنها لا تظهر في المتجر.

---

## 🔍 الأسباب المحتملة

### 1. الزر مخفي افتراضيًا
الزر له `opacity-0 group-hover:opacity-100` - يعني:
- ✅ الزر موجود في الكود
- ⚠️ مخفي افتراضيًا (`opacity-0`)
- ✅ يظهر فقط عند hover على بطاقة المنتج (`group-hover:opacity-100`)

### 2. الإعدادات لا تُجلب بشكل صحيح
- تم إصلاح `process is not defined` لكن قد تكون هناك مشكلة أخرى
- الإعدادات قد تكون `null` أو `undefined`

### 3. الشرط لا يعمل
- `storefrontSettings?.quickViewEnabled` قد يكون `false` أو `undefined`

---

## ✅ خطوات التصحيح

### 1. فتح Developer Console (F12)
- اذهب إلى Console tab
- ابحث عن:
  - `✅ [Shop] Storefront settings loaded:`
  - `✅ [Shop] Quick View is ENABLED`
  - `⚠️ [Shop] Quick View is DISABLED`

### 2. التحقق من الإعدادات
- افتح Network tab
- ابحث عن: `/public/storefront-settings/`
- تحقق من الـ response:
  - `quickViewEnabled: true` ✅
  - `quickViewEnabled: false` ❌

### 3. التحقق من الزر
- مرر الماوس على بطاقة المنتج
- يجب أن يظهر زر "معاينة سريعة" في الزاوية العلوية اليسرى
- إذا لم يظهر، تحقق من:
  - `storefrontSettings` ليس `null`
  - `quickViewEnabled` هو `true`
  - `group` class موجود على بطاقة المنتج

---

## 🔧 الإصلاحات المطبقة

1. ✅ إضافة console logs للتحقق من الإعدادات
2. ✅ إضافة `z-10` للزر لضمان ظهوره فوق العناصر الأخرى
3. ✅ إضافة `aria-label` للزر

---

## 📝 ملاحظات

- الزر يظهر فقط عند **hover** على بطاقة المنتج
- إذا كنت تريد أن يظهر دائماً، يجب تغيير `opacity-0` إلى `opacity-100`
- تأكد من أن الإعدادات تم حفظها بشكل صحيح

---

## 🎯 الحل السريع

إذا كنت تريد أن يظهر الزر دائماً (بدون hover):

```tsx
className="absolute top-2 left-2 bg-white bg-opacity-90 hover:bg-opacity-100 p-2 rounded-full shadow-lg opacity-100 z-10"
```

بدلاً من:
```tsx
className="absolute top-2 left-2 bg-white bg-opacity-90 hover:bg-opacity-100 p-2 rounded-full shadow-lg opacity-0 group-hover:opacity-100 transition-opacity z-10"
```

