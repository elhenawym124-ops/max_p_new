# 🛒 ميزة إظهار/إخفاء زر "أضف للسلة" - توثيق

**تاريخ الإنشاء:** 2024-12-19

---

## 📋 ملخص

تم إضافة ميزة جديدة تسمح بإظهار أو إخفاء زر "أضف للسلة" في صفحة المنتج من خلال صفحة إضافة/تعديل المنتج.

---

## ✅ ما تم إنجازه

### 1. **إضافة حقل جديد في قاعدة البيانات**
- **الحقل:** `showAddToCartButton` (Boolean)
- **الموقع:** `backend/prisma/schema.prisma` → `model Product`
- **القيمة الافتراضية:** `true` (مفعّل افتراضياً)
- **الوصف:** يتحكم في إظهار/إخفاء زر "أضف للسلة" في صفحة المنتج

```prisma
showAddToCartButton Boolean @default(true) // 🛒 إظهار زر إضافة للسلة في صفحة المنتج
```

---

### 2. **تحديث صفحة تفاصيل المنتج (ProductDetails)**
- **الملف:** `frontend/src/pages/storefront/ProductDetails.tsx`
- **التغيير:** تحديث شرط إظهار زر "أضف للسلة" ليتحقق من `showAddToCartButton`

**قبل:**
```typescript
{product.enableCheckoutForm === false && (
  // زر أضف للسلة
)}
```

**بعد:**
```typescript
{product.showAddToCartButton !== false && product.enableCheckoutForm === false && (
  // زر أضف للسلة
)}
```

**المنطق:**
- يظهر زر "أضف للسلة" فقط إذا:
  1. `showAddToCartButton !== false` (مفعّل)
  2. `enableCheckoutForm === false` (فورم الشيك أوت غير مفعّل)

---

### 3. **إضافة خيار في صفحة إضافة منتج جديد**
- **الملف:** `frontend/src/pages/products/ProductNew.tsx`
- **التغييرات:**
  - إضافة `showAddToCartButton: boolean` في `ProductFormData` interface
  - إضافة `showAddToCartButton: true` في القيم الافتراضية
  - إضافة checkbox في النموذج
  - إرسال الحقل عند حفظ المنتج

**الموقع في النموذج:**
- بعد checkbox "تفعيل فورم الشيك أوت"
- Label: "إظهار زر إضافة للسلة في صفحة المنتج"
- Tooltip: "(عند إلغاء التفعيل، لن يظهر زر "أضف للسلة" في صفحة المنتج)"

---

### 4. **إضافة خيار في صفحة تعديل المنتج**
- **الملف:** `frontend/src/pages/products/ProductEditNew.tsx`
- **التغييرات:**
  - إضافة `showAddToCartButton: true` في `formData` state
  - تحميل القيمة من قاعدة البيانات عند تحميل المنتج
  - إضافة checkbox في النموذج
  - إرسال الحقل عند حفظ المنتج

**الموقع في النموذج:**
- بعد checkbox "🛒 تفعيل فورم الشيك أوت"
- Label: "🛒 إظهار زر إضافة للسلة"
- Tooltip: "(عند إلغاء التفعيل، لن يظهر زر "أضف للسلة" في صفحة المنتج)"

---

## 🔍 كيفية الاستخدام

### للمستخدم (صاحب المتجر):

1. **إضافة منتج جديد:**
   - اذهب إلى `/products/new`
   - في قسم "إعدادات المنتج"، ستجد checkbox "إظهار زر إضافة للسلة في صفحة المنتج"
   - ✅ مفعّل افتراضياً
   - ❌ يمكنك إلغاء التفعيل لإخفاء الزر

2. **تعديل منتج موجود:**
   - اذهب إلى `/products/:id/edit`
   - في قسم "إعدادات المنتج"، ستجد checkbox "🛒 إظهار زر إضافة للسلة"
   - ✅ يمكنك تفعيل/إلغاء التفعيل حسب الحاجة

3. **النتيجة في المتجر:**
   - إذا كان `showAddToCartButton = true` و `enableCheckoutForm = false`:
     - ✅ يظهر زر "أضف للسلة" في صفحة المنتج
   - إذا كان `showAddToCartButton = false`:
     - ❌ لا يظهر زر "أضف للسلة" في صفحة المنتج

---

## 📊 العلاقة مع `enableCheckoutForm`

### المنطق الحالي:
```typescript
// يظهر زر "أضف للسلة" فقط إذا:
product.showAddToCartButton !== false && product.enableCheckoutForm === false
```

### السيناريوهات:

| `showAddToCartButton` | `enableCheckoutForm` | النتيجة |
|----------------------|---------------------|---------|
| ✅ `true` | ❌ `false` | ✅ يظهر زر "أضف للسلة" |
| ✅ `true` | ✅ `true` | ❌ لا يظهر (لأن فورم الشيك أوت مفعّل) |
| ❌ `false` | ❌ `false` | ❌ لا يظهر (لأن الزر معطّل) |
| ❌ `false` | ✅ `true` | ❌ لا يظهر |

**ملاحظة:** إذا كان `enableCheckoutForm = true`، لن يظهر زر "أضف للسلة" حتى لو كان `showAddToCartButton = true`، لأن المنتج يستخدم فورم الشيك أوت المباشر.

---

## 🗄️ قاعدة البيانات

### Migration:
تم تطبيق التغييرات على قاعدة البيانات باستخدام:
```bash
npx prisma db push
```

### الحقل الجديد:
- **الاسم:** `showAddToCartButton`
- **النوع:** `Boolean`
- **القيمة الافتراضية:** `true`
- **الموقع:** جدول `Product`

---

## 📝 الملفات المعدلة

1. ✅ `backend/prisma/schema.prisma` - إضافة الحقل
2. ✅ `frontend/src/pages/storefront/ProductDetails.tsx` - تحديث شرط الإظهار
3. ✅ `frontend/src/pages/products/ProductNew.tsx` - إضافة checkbox
4. ✅ `frontend/src/pages/products/ProductEditNew.tsx` - إضافة checkbox

---

## 🧪 الاختبار

### سيناريوهات الاختبار:

1. **إضافة منتج جديد مع تفعيل الزر:**
   - ✅ تأكد من أن checkbox "إظهار زر إضافة للسلة" مفعّل
   - ✅ احفظ المنتج
   - ✅ افتح صفحة المنتج في المتجر
   - ✅ تأكد من ظهور زر "أضف للسلة"

2. **إضافة منتج جديد مع إلغاء تفعيل الزر:**
   - ✅ ألغِ تفعيل checkbox "إظهار زر إضافة للسلة"
   - ✅ احفظ المنتج
   - ✅ افتح صفحة المنتج في المتجر
   - ✅ تأكد من عدم ظهور زر "أضف للسلة"

3. **تعديل منتج موجود:**
   - ✅ افتح صفحة تعديل منتج
   - ✅ ألغِ تفعيل checkbox "🛒 إظهار زر إضافة للسلة"
   - ✅ احفظ التغييرات
   - ✅ افتح صفحة المنتج في المتجر
   - ✅ تأكد من عدم ظهور زر "أضف للسلة"

---

## 🎯 الفوائد

1. ✅ **مرونة أكبر:** يمكن إخفاء زر "أضف للسلة" للمنتجات التي لا تريد بيعها مباشرة
2. ✅ **تحكم كامل:** يمكن التحكم في كل منتج على حدة
3. ✅ **سهولة الاستخدام:** checkbox بسيط في صفحة إضافة/تعديل المنتج
4. ✅ **قيمة افتراضية:** الزر مفعّل افتراضياً، مما يضمن عدم كسر المنتجات الموجودة

---

## 🔮 تحسينات مستقبلية محتملة

1. **إضافة خيار عام:** إضافة خيار في إعدادات المتجر لإخفاء زر "أضف للسلة" لجميع المنتجات
2. **رسالة مخصصة:** إضافة رسالة مخصصة تظهر بدلاً من زر "أضف للسلة" عند إخفائه
3. **زر بديل:** إضافة زر بديل (مثل "اتصل بنا" أو "اطلب عرض سعر") عند إخفاء زر "أضف للسلة"

---

**تاريخ آخر تحديث:** 2024-12-19


