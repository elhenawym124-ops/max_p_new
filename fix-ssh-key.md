# 🔑 إصلاح مشكلة SSH Key

## المشكلة:
`ssh: no key found` - يعني أن SSH key غير صحيح في GitHub Secrets

## الحل:

### 1. تحقق من المفتاح الخاص:

```powershell
# عرض المفتاح الخاص
type C:\Users\pc\.ssh\id_ed25519
```

**يجب أن يبدأ بـ:**
```
-----BEGIN OPENSSH PRIVATE KEY-----
```

**ويجب أن ينتهي بـ:**
```
-----END OPENSSH PRIVATE KEY-----
```

### 2. انسخ المفتاح كاملاً:

1. افتح: `C:\Users\pc\.ssh\id_ed25519`
2. **انسخ كل المحتوى** (من `-----BEGIN` إلى `-----END`)
3. **مهم جداً:** لا تنس أي سطر، حتى الأسطر الفارغة

### 3. أضف المفتاح في GitHub:

1. اذهب إلى: https://github.com/elhenawym124-ops/max_p_new/settings/secrets/actions
2. اضغط على `SERVER_SSH_KEY`
3. اضغط "Update"
4. **الصق المفتاح كاملاً** (من `-----BEGIN` إلى `-----END`)
5. تأكد من عدم وجود مسافات إضافية في البداية أو النهاية
6. احفظ

### 4. تحقق من المفتاح:

```powershell
# جرب الاتصال
ssh -i C:\Users\pc\.ssh\id_ed25519 root@153.92.223.119
```

إذا نجح = المفتاح صحيح ✅

---

## ملاحظات مهمة:

- ✅ المفتاح يجب أن يكون **كاملاً** (كل الأسطر)
- ✅ لا تنس `-----BEGIN` و `-----END`
- ✅ لا تضيف مسافات إضافية
- ✅ لا تكسر الأسطر

---

## إذا استمرت المشكلة:

جرب إنشاء مفتاح جديد:

```powershell
# حذف المفاتيح القديمة (اختياري)
Remove-Item C:\Users\pc\.ssh\id_ed25519*
ssh-keygen -t ed25519 -C "github-actions" -f C:\Users\pc\.ssh\id_ed25519 -N '""'

# رفع المفتاح الجديد
$pubkey = Get-Content C:\Users\pc\.ssh\id_ed25519.pub
ssh root@153.92.223.119 "mkdir -p ~/.ssh && echo '$pubkey' >> ~/.ssh/authorized_keys && chmod 600 ~/.ssh/authorized_keys"

# نسخ المفتاح الخاص إلى GitHub Secrets
type C:\Users\pc\.ssh\id_ed25519
```

