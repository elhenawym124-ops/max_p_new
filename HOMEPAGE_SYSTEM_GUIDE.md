# 🏠 نظام إدارة الصفحة الرئيسية - دليل الإعداد والاستخدام

## 📋 نظرة عامة

تم إنشاء نظام متكامل لإدارة الصفحات الرئيسية للمتجر مع إمكانية:
- إنشاء صفحات رئيسية متعددة
- تصميم حديث مستوحى من Elementor/PressMart
- محرر سهل الاستخدام مع أقسام قابلة للسحب والإفلات
- قالب تجريبي جاهز للاستخدام
- معاينة وتفعيل الصفحات

## 🚀 خطوات الإعداد

### 1. تشغيل Migration للـ Database

```bash
cd backend
npx prisma migrate dev --name add_homepage_templates
```

أو إذا كنت تريد فقط تحديث Prisma Client:

```bash
npx prisma generate
```

### 2. إعادة تشغيل الـ Backend

```bash
npm run dev
```

### 3. إعادة تشغيل الـ Frontend

```bash
cd frontend
npm run dev
```

## 📍 الوصول للنظام

### من لوحة التحكم:
1. اذهب إلى **إعدادات المتجر** (Store Settings)
2. اضغط على تبويبة **الصفحة الرئيسية** (أول تبويبة)
3. اضغط على **إدارة الصفحات الرئيسية**

### أو مباشرة:
```
/settings/homepage
```

## ✨ المميزات الرئيسية

### 1. إدارة القوالب
- **عرض جميع القوالب**: قائمة بجميع الصفحات الرئيسية المنشأة
- **القالب النشط**: يظهر بوضوح أي صفحة نشطة حالياً
- **إنشاء قالب جديد**: إنشاء صفحة من الصفر
- **قالب تجريبي**: إنشاء صفحة جاهزة بتصميم حديث

### 2. محرر الصفحات
- **إضافة أقسام**: 6 أنواع من الأقسام:
  - 🎯 **قسم البطل (Hero)**: صورة كبيرة مع عنوان وزر
  - ⭐ **المميزات (Features)**: عرض مميزات المتجر
  - 🛍️ **المنتجات (Products)**: عرض المنتجات المميزة
  - 📢 **بانر (Banner)**: بانر إعلاني
  - 📁 **الفئات (Categories)**: عرض فئات المنتجات
  - 💬 **التقييمات (Testimonials)**: آراء العملاء

- **ترتيب الأقسام**: تحريك الأقسام لأعلى أو لأسفل
- **تعديل الأقسام**: تخصيص كل قسم بشكل منفصل
- **حذف الأقسام**: إزالة الأقسام غير المرغوبة

### 3. إعدادات الصفحة
- **عرض الحاوية**: كامل أو محدود
- **المسافات**: مضغوط، عادي، أو واسع
- **الحركات**: تفعيل/تعطيل الأنيميشن

### 4. العمليات المتاحة
- ✅ **تفعيل**: جعل الصفحة هي الرئيسية النشطة
- ✏️ **تعديل**: تحرير محتوى الصفحة
- 👁️ **معاينة**: عرض الصفحة قبل التفعيل
- 📋 **نسخ**: إنشاء نسخة من الصفحة
- 🗑️ **حذف**: حذف الصفحة (لا يمكن حذف الصفحة النشطة)

## 🎨 القالب التجريبي (Demo Template)

القالب التجريبي يحتوي على:

### الأقسام:
1. **Hero Section**: صورة رئيسية مع عنوان ترحيبي
2. **Features**: 4 مميزات (شحن سريع، دفع آمن، دعم 24/7، إرجاع مجاني)
3. **Products**: عرض 8 منتجات مميزة
4. **Banner**: بانر عروض خاصة
5. **Categories**: عرض الفئات
6. **Testimonials**: 3 تقييمات من العملاء

### التصميم:
- ألوان حديثة (Indigo/Purple)
- صور من Unsplash
- تصميم متجاوب (Responsive)
- أيقونات من Heroicons

## 🔧 API Endpoints

### Protected Routes (تحتاج Authentication):

```javascript
GET    /api/v1/homepage/templates          // جلب جميع القوالب
GET    /api/v1/homepage/active             // جلب القالب النشط
POST   /api/v1/homepage/templates          // إنشاء قالب جديد
POST   /api/v1/homepage/templates/demo     // إنشاء القالب التجريبي
PUT    /api/v1/homepage/templates/:id      // تحديث قالب
PUT    /api/v1/homepage/templates/:id/activate  // تفعيل قالب
POST   /api/v1/homepage/templates/:id/duplicate // نسخ قالب
DELETE /api/v1/homepage/templates/:id      // حذف قالب
```

### Public Routes (للواجهة الأمامية):

```javascript
GET    /api/v1/homepage/public/:companyId  // جلب القالب النشط للعرض العام
```

## 📊 Database Schema

```prisma
model HomepageTemplate {
  id          String   @id @default(cuid())
  companyId   String
  name        String
  description String?  @db.Text
  content     String   @db.LongText  // JSON content
  thumbnail   String?  @db.Text
  isActive    Boolean  @default(false)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  company     Company  @relation(...)
}
```

## 🎯 الخطوات التالية المقترحة

### 1. عرض الصفحة في الواجهة الأمامية
إنشاء مكون لعرض الصفحة الرئيسية النشطة في الـ Storefront

### 2. مكونات الأقسام
إنشاء مكونات React لكل نوع من أنواع الأقسام:
- `HeroSection.tsx`
- `FeaturesSection.tsx`
- `ProductsSection.tsx`
- `BannerSection.tsx`
- `CategoriesSection.tsx`
- `TestimonialsSection.tsx`

### 3. محرر متقدم
- إضافة محرر نصوص غني (Rich Text Editor)
- رفع الصور مباشرة
- اختيار المنتجات من قائمة
- اختيار الألوان

### 4. معاينة حية
- معاينة التغييرات في الوقت الفعلي
- عرض الصفحة كما ستظهر للعملاء

## 📝 ملاحظات مهمة

1. **القالب النشط**: يمكن أن يكون هناك قالب نشط واحد فقط في كل مرة
2. **الحذف**: لا يمكن حذف القالب النشط، يجب تفعيل قالب آخر أولاً
3. **المحتوى**: يتم حفظ المحتوى بصيغة JSON في قاعدة البيانات
4. **الأمان**: جميع العمليات محمية بـ Authentication

## 🐛 استكشاف الأخطاء

### خطأ في Migration:
```bash
npx prisma migrate reset
npx prisma migrate dev
```

### خطأ في Prisma Client:
```bash
npx prisma generate
```

### خطأ في الـ API:
تأكد من:
- تشغيل الـ Backend
- صحة الـ Authentication Token
- صحة الـ companyId

## 📞 الدعم

للمساعدة أو الاستفسارات، يمكنك:
1. مراجعة الكود في:
   - Backend: `backend/controller/homepageController.js`
   - Frontend: `frontend/src/pages/settings/HomepageSettings.tsx`
   - Service: `frontend/src/services/homepageService.ts`

2. فحص الـ Console للأخطاء
3. مراجعة الـ Network Tab في Developer Tools

---

## ✅ تم إنشاء الملفات التالية:

### Backend:
- ✅ `backend/controller/homepageController.js`
- ✅ `backend/routes/homepageRoutes.js`
- ✅ `backend/prisma/schema.prisma` (تم التحديث)
- ✅ `backend/server.js` (تم التحديث)

### Frontend:
- ✅ `frontend/src/services/homepageService.ts`
- ✅ `frontend/src/pages/settings/HomepageSettings.tsx`
- ✅ `frontend/src/pages/settings/HomepageEditor.tsx`
- ✅ `frontend/src/pages/settings/StoreSettings.tsx` (تم التحديث)
- ✅ `frontend/src/App.tsx` (تم التحديث)

---

**تم بنجاح! 🎉**

النظام جاهز للاستخدام بعد تشغيل Migration وإعادة تشغيل الـ Backend والـ Frontend.
