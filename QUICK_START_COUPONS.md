# 🎟️ دليل سريع - نظام الكوبونات

## ✅ تم إنشاء 5 كوبونات لشركة التسويق

---

## 🎯 الكوبونات المتاحة

### 1. SUMMER2024
```
📌 خصم 20% على جميع المنتجات
💰 الحد الأدنى: 100 جنيه
🎁 الحد الأقصى للخصم: 200 جنيه
👥 لجميع العملاء
```

### 2. NEWCUSTOMER50
```
📌 خصم 50 جنيه للعملاء الجدد
💰 الحد الأدنى: 200 جنيه
👥 للعملاء الجدد فقط
```

### 3. FREESHIP
```
📌 شحن مجاني
💰 الحد الأدنى: 300 جنيه
👥 لجميع العملاء
```

### 4. VIP30
```
📌 خصم 30% حصري
💰 الحد الأدنى: 500 جنيه
🎁 الحد الأقصى للخصم: 500 جنيه
👥 لعملاء VIP فقط
```

### 5. FLASH100
```
📌 خصم 100 جنيه - عرض محدود
💰 الحد الأدنى: 500 جنيه
⏰ صالح لمدة 7 أيام فقط
👥 لجميع العملاء
```

---

## 🚀 كيفية الاستخدام

### 1. عرض جميع الكوبونات
```bash
GET http://localhost:3007/api/v1/coupons
Authorization: Bearer YOUR_TOKEN
```

### 2. التحقق من كوبون
```bash
POST http://localhost:3007/api/v1/coupons/validate
Authorization: Bearer YOUR_TOKEN
Content-Type: application/json

{
  "code": "SUMMER2024",
  "orderAmount": 500
}
```

**النتيجة:**
```json
{
  "success": true,
  "data": {
    "coupon": {
      "code": "SUMMER2024",
      "name": "خصم الصيف 2024",
      "type": "PERCENTAGE",
      "value": 20
    },
    "discountAmount": 100,
    "finalAmount": 400
  }
}
```

### 3. إنشاء كوبون جديد
```bash
POST http://localhost:3007/api/v1/coupons
Authorization: Bearer YOUR_TOKEN
Content-Type: application/json

{
  "code": "NEWYEAR2025",
  "name": "خصم رأس السنة",
  "description": "خصم 25% احتفالاً برأس السنة",
  "type": "PERCENTAGE",
  "value": 25,
  "minOrderAmount": 150,
  "maxDiscountAmount": 300,
  "usageLimit": 200,
  "userUsageLimit": 2,
  "validFrom": "2025-01-01T00:00:00Z",
  "validTo": "2025-01-31T23:59:59Z",
  "isActive": true,
  "customerSegments": ["all"]
}
```

---

## 📱 الوصول من الواجهة

### افتح صفحة الكوبونات:
```
http://localhost:3000/coupons
```

### المميزات المتاحة:
- ✅ عرض جميع الكوبونات
- ✅ فلترة حسب الحالة والنوع
- ✅ إنشاء كوبون جديد
- ✅ تعديل الكوبونات
- ✅ حذف الكوبونات
- ✅ عرض إحصائيات الاستخدام

---

## 🧪 اختبار الكوبونات

### مثال 1: طلب بقيمة 500 جنيه مع SUMMER2024
```
قيمة الطلب: 500 جنيه
الكوبون: SUMMER2024 (خصم 20%)
الخصم: 100 جنيه (20% من 500)
المبلغ النهائي: 400 جنيه ✅
```

### مثال 2: طلب بقيمة 1000 جنيه مع SUMMER2024
```
قيمة الطلب: 1000 جنيه
الكوبون: SUMMER2024 (خصم 20%)
الخصم المحسوب: 200 جنيه (20% من 1000)
الحد الأقصى: 200 جنيه
الخصم الفعلي: 200 جنيه ✅
المبلغ النهائي: 800 جنيه
```

### مثال 3: طلب بقيمة 250 جنيه مع NEWCUSTOMER50
```
قيمة الطلب: 250 جنيه
الكوبون: NEWCUSTOMER50 (خصم 50 جنيه)
الخصم: 50 جنيه
المبلغ النهائي: 200 جنيه ✅
```

### مثال 4: طلب بقيمة 350 جنيه مع FREESHIP
```
قيمة الطلب: 350 جنيه
الكوبون: FREESHIP (شحن مجاني)
الخصم: قيمة الشحن
المبلغ النهائي: 350 جنيه + شحن مجاني ✅
```

---

## ⚠️ حالات الرفض

### 1. الطلب أقل من الحد الأدنى
```
الطلب: 80 جنيه
الكوبون: SUMMER2024 (حد أدنى 100 جنيه)
النتيجة: ❌ "الحد الأدنى لقيمة الطلب هو 100 جنيه"
```

### 2. الكوبون منتهي الصلاحية
```
التاريخ: 2026-01-01
الكوبون: FLASH100 (صالح حتى 2025-11-29)
النتيجة: ❌ "الكوبون منتهي الصلاحية"
```

### 3. تم استنفاد عدد الاستخدامات
```
الكوبون: FLASH100 (30 استخدام)
الاستخدام الحالي: 30/30
النتيجة: ❌ "تم استنفاد عدد مرات استخدام الكوبون"
```

---

## 📊 الإحصائيات

### عرض إحصائيات الكوبونات:
```bash
GET http://localhost:3007/api/v1/coupons/stats
Authorization: Bearer YOUR_TOKEN
```

**النتيجة:**
```json
{
  "success": true,
  "data": {
    "totalCoupons": 5,
    "activeCoupons": 5,
    "totalUsages": 0,
    "totalDiscount": 0
  }
}
```

---

## 🎨 أنواع الكوبونات

### PERCENTAGE (نسبة مئوية)
```javascript
{
  "type": "PERCENTAGE",
  "value": 20,  // 20%
  "maxDiscountAmount": 200  // اختياري
}
```

### FIXED (مبلغ ثابت)
```javascript
{
  "type": "FIXED",
  "value": 50  // 50 جنيه
}
```

### FREE_SHIPPING (شحن مجاني)
```javascript
{
  "type": "FREE_SHIPPING",
  "value": 0
}
```

---

## 👥 فئات العملاء

```javascript
{
  "customerSegments": ["all"]      // جميع العملاء
  "customerSegments": ["new"]      // العملاء الجدد
  "customerSegments": ["VIP"]      // عملاء VIP
  "customerSegments": ["regular"]  // العملاء العاديين
  "customerSegments": ["new", "VIP"]  // أكثر من فئة
}
```

---

## 🔄 تكامل مع الطلبات

### عند إنشاء طلب:
1. العميل يدخل كود الكوبون
2. النظام يتحقق من صلاحية الكوبون
3. يتم حساب الخصم
4. يتم تطبيق الخصم على الطلب
5. يتم تسجيل استخدام الكوبون

```javascript
// 1. التحقق من الكوبون
const validation = await fetch('/api/v1/coupons/validate', {
  method: 'POST',
  body: JSON.stringify({
    code: 'SUMMER2024',
    orderAmount: 500,
    customerId: 'customer_id'
  })
});

// 2. إنشاء الطلب مع الخصم
const order = await createOrder({
  items: [...],
  subtotal: 500,
  discount: validation.data.discountAmount,
  total: validation.data.finalAmount
});

// 3. تسجيل استخدام الكوبون
await fetch('/api/v1/coupons/apply', {
  method: 'POST',
  body: JSON.stringify({
    couponId: validation.data.coupon.id,
    orderId: order.id,
    customerId: 'customer_id',
    orderAmount: 500,
    discountAmount: validation.data.discountAmount
  })
});
```

---

## ✅ الحالة

**النظام جاهز للاستخدام الفوري!**

- ✅ قاعدة البيانات جاهزة
- ✅ API جاهز
- ✅ 5 كوبونات تجريبية متاحة
- ✅ الواجهة جاهزة
- ✅ التوثيق كامل

**ابدأ الآن:** افتح `http://localhost:3000/coupons`
